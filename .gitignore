# Secrets — refresh tokens, API keys etc. NEVER commit.
data/firebase_auth.json
.env
.env.local
.env.*.local

# Personal / user-specific data — every user has their own state.
# These get auto-generated from Firestore + DVV scrapes on first run.
# (data/players_all.json IS tracked — it's the curated historical roster.)
data/players_available.json
data/players_season.json
data/players_season_*.json
data/tournament_sim.json
data/bracket_m.json
data/bracket_f.json

# Disk caches (DVV / GBT bracket / H2H / Firestore-Snapshot)
data/.cache/

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Node (only present because some dev tooling was installed locally)
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/

# Claude Code working dir (transcripts, agent state, etc.)
.claude/
